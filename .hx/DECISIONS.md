# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001-df6x5t | arch | How to handle the GSD backward-compat migration code | Preserve migrate-gsd-to-hx.ts untouched | This file handles .gsd/ → .hx/ directory migration for existing users upgrading from the old GSD binary. Renaming it would break the migration path. | Yes | collaborative |
| D002 | M001-df6x5t | scope | How to handle native platform package names (@gsd-build/engine-*) | Rename in source only, no npm registry publish | npm registry operations are out of scope for this rename milestone. Source files get @hyperlab/engine-* names but no actual publish happens. | Yes | collaborative |
| D003 | M001-df6x5t/S01 | scope | Whether packages/native gsd_engine binary path strings are in S01 scope | Exclude gsd_engine binary path strings from S01; defer to S04 (Native Rust Engine) | The gsd_engine strings in packages/native/src/native.ts are binary artifact file names (gsd_engine.*.node), not TypeScript type names. These belong to R004/R008 (native engine rename) which S04 owns. S01 only renames the TS-side type declaration batchParseGsdFiles in the interface. | Yes | agent |
| D004 | M001/S01/T02 | rename-strategy | How to handle native runtime call string that cannot be excluded by the T02 verification grep pattern | Keep (native as Record&lt;string, Function&gt;).batchParseGsdFiles( unchanged in hx-parser/index.ts line 83 until S04 renames the Rust binary; accept count=1 in verification | The Rust binary still exports batchParseGsdFiles. Renaming the runtime call string would cause a runtime crash. The verification exclusion pattern native\.batchParseGsdFiles was designed to exclude this call but doesn't match the cast syntax. Correctness trumps grep count. S04 will rename both the binary and this call string together. | Yes | agent |
| D005 | M001-df6x5t/S04 planning | M001-df6x5t/S04 | What npm scope to use for native platform packages (@gsd-build/engine-* replacement) | Use @hx-build/engine-* (not @hyperlab/engine-*) | native.ts line 39 already uses `@hx-build/engine-${packageSuffix}` as the require path. The platform package.json names must match this exact scope for the runtime loader to find them. D002 originally said @hyperlab but the codebase already chose @hx-build. | Yes | agent |
| D006 | M002-yle1ri | convention | Verification level for ported fixes | Typecheck + build + full test suite after each slice | Full test suite catches regressions that typecheck alone would miss — naming adaptation bugs are often runtime errors | No | human |
| D007 | M002-yle1ri/S01 | architecture | Task decomposition strategy for S01 (16 upstream state/DB fixes) | 5 tasks grouped by file affinity and risk: T01=unit-ownership SQLite migration (isolated, highest complexity), T02=state.ts triple fix + VACUUM (same reconciliation codepath), T03=data safety guards (small independent fixes), T04=repo-identity relocation (isolated high-risk hash change), T05=scatter fixes (6 small fixes grouped) | Grouping by file affinity minimizes context needed per task. Isolating unit-ownership (T01) and repo-identity (T04) because they're the most complex structural changes with API/hash changes. State.ts fixes share test infrastructure so bundle together. T05 groups 6 tiny fixes (2-20 lines each) that individually don't warrant separate tasks. | Yes | agent |
| D008 |  | architecture | Task decomposition strategy for S02 (21 upstream worktree/git + auto-mode fixes) | 4 tasks grouped by file affinity: T01=auto-worktree.ts (5 fixes, most complex single file), T02=git subsystem (6 fixes across paths/git-service/worktree-manager/worktree-resolver/parallel-merge), T03=auto-mode dispatch (5 fixes across cli/agent-end-recovery/phases/write-gate/register-hooks), T04=metadata+verification (5 fixes across preferences/prompts/captures/triage-resolution + full typecheck+test run) | auto-worktree.ts (1726 lines, 5 fixes) needs its own task due to complexity and position sensitivity. Git subsystem files share conceptual affinity (paths, worktree lifecycle, merge). Auto-mode dispatch files share runtime flow affinity (CLI→dispatch→loop→hooks). Metadata/preferences/prompts/captures are smaller, independent changes that batch well with the final verification pass. The absorbSnapshotCommits fix (upstream #0402e46c1) is N/A — the method doesn't exist in hx-ai, reducing effective fixes from 21 to 20. | Yes | agent |
| D009 | M002-yle1ri/S04/T04 | template-engine | Template variable substitution method in prompt-loader.ts | Use content.split(`{{key}}`).join(value) instead of content.replaceAll(`{{key}}`, value) | String.replaceAll treats $ in the replacement string as a pattern ($1, $&, $` etc.) causing explosion when replacement values contain dollar signs. split().join() treats the replacement string as a literal always. | Yes | agent |
| D010 | M002-yle1ri/S04/T04 | TUI/performance | Prevent TUI render loop CPU burn in non-TTY environments | isTTY: boolean added to Terminal interface; ProcessTerminal returns process.stdout.isTTY ?? false; TUI.start() and requestRender() guard on !isTTY | Non-TTY environments (CI, piped scripts) were spinning the render loop even though there is no terminal to render to. The Terminal interface is the correct abstraction layer for this guard; RemoteTerminal always returns true as it is always TTY-like. | Yes | agent |
| D011 | M002-yle1ri/S05/T04 | pattern | Where to place integration test files for compile-tests.mjs to pick them up | Place test files in flat tests/ directory, not tests/integration/ subdirectory | compile-tests.mjs has an explicit SKIP_DIRS exclusion that skips the integration/ subdirectory. Tests placed in tests/integration/ will not be compiled and will silently not run. All new test files must go in the flat tests/ directory. | Yes | agent |
| D012 | M002-yle1ri/S05/T03 | pattern | Which customType string to use for forensics context injection in system-context.ts | Use customType hx-forensics (matching hx-guided-context pattern) | system-context.ts already used hx-guided-context (not gsd-guided-context). New forensics injection uses hx-forensics for consistency with HX naming convention, even though the upstream gsd-2 code used gsd-forensics. | Yes | agent |
| D013 | M002-yle1ri/S05/T02 | pattern | Return type semantics for getDbCompletionCounts when DB is not accessible | getDbCompletionCounts returns null (not 0) when DB unavailable | Distinguishing null (DB unavailable) from {milestones:0, slices:0, tasks:0} (DB available but nothing completed) is important for accurate forensics reporting — null signals to formatReportForPrompt that DB counts should not be displayed at all. | Yes | agent |
| D014 | M002-yle1ri/S06 verification gate | test-infrastructure | Extension smoke test extensionsDir for compiled test runner | Use dist-test/src/resources/extensions/ when available; convert .ts entry paths to .js before import() | Node.js 23.4 rejects absolute file:// .ts imports before hooks can intercept them. The compiled output in dist-test has .js files that Node can load directly. | Yes | agent |
| D015 | M002-yle1ri/S06 verification gate | test-infrastructure | RTK test isolation from HX_RTK_DISABLED environment variable | Add withRtkEnabled() helper to unset HX_RTK_DISABLED in test setup and restore in finally | The test environment sets HX_RTK_DISABLED=1 which causes isRtkEnabled() to return false, silently causing all RTK functions to return null/passthrough without running the fake binary. | Yes | agent |
| D016 | M003-ttxmyu planning | architecture | How to handle upstream gsd-db.ts refactor vs existing hx-db.ts | Single unified hx-db.ts — reconcile gsd-db.ts additions into existing hx-db.ts, no separate file | Creating a duplicate DB abstraction layer would cause silent state divergence. hx-db.ts is the established file; gsd-db.ts additions (if any are net-new) get merged in. | No | collaborative |
| D017 | M003-ttxmyu planning | convention | Env var naming for slice-level parallelism lock | Rename GSD_SLICE_LOCK → HX_SLICE_LOCK throughout slice parallelism port | Consistent with all prior GSD→HX env var renames. HX_SLICE_LOCK is the correct hx-ai name per the naming convention established in M001. | No | agent |
| D018 | M003-ttxmyu planning | scope | M003-ttxmyu port scope: bugfix-only vs full (fix + feat + refactor) | Port scope includes both bugfix and feature commits (v2.59.0 deferred features + all v2.59.0→v2.63.0 commits) | User explicitly confirmed inclusion of v2.59.0 added-section features (R015 deferred from M002) plus all v2.59.0→v2.63.0 delta. Full port, not bugfix-only. | No | human |
| D019 | M003-ttxmyu/S06 planning | implementation | How to implement preferences bootstrap model fix (cluster 5) when resolveDefaultSessionModel doesn't exist in preferences-models.ts | Use resolveModelWithFallbacksForUnit('default') as the equivalent — it resolves the preferred model for the 'default' unit type and returns a ResolvedModelConfig with provider info | The upstream function resolveDefaultSessionModel is not present in hx-ai's preferences-models.ts. resolveModelWithFallbacksForUnit is the closest equivalent and covers the same use case: resolving the user-configured default session model including provider info needed to build startModelSnapshot. | Yes | agent |
| D020 | M004-erchk5/planning | scope | How to handle fea9d72de commit in M004 port range | Skip fea9d72de (slice-level parallelism) — already ported in M003/S02 | fea9d72de is in the v2.63.0→v2.64.0 range but was explicitly ported in M003/S02 as slice-parallel-orchestrator.ts, slice-parallel-conflict.ts, slice-parallel-eligibility.ts. Re-applying would create duplicate code and test conflicts. | No | agent |
| D021 | M004-erchk5/planning | arch | Directory structure for LLM safety harness new files | safety/ goes under src/resources/extensions/hx/safety/ with HX naming (SafetyHarnessConfig, not GsdSafetyConfig) | Mirrors upstream gsd/safety/ pattern but under the hx/ extension directory. All type names and preference keys use HX prefix to maintain naming invariant from M001. | No | agent |
| D022 | M004-erchk5/S02/T02 | provider | How to register keyless Ollama provider so isProviderRequestReady passes | pi.registerProvider('ollama', {authMode:'none', api:'ollama-chat', baseUrl:'http://localhost:11434'}) in register-extension.ts | isProviderRequestReady checks for an API key unless authMode:'none' is set. Without this registration, discovered Ollama models would fail the ready check and silently not route. | Yes | agent |
| D023 | M004-erchk5/S02/T02 | routing | How to implement flat-rate routing guard for zero-cost providers | FLAT_RATE_PREFIXES array + isFlatRateModel() export in model-router.ts; early return in resolveModelForComplexity | Array-based prefix check (rather than hardcoded if/else) makes it trivial to add future flat-rate providers (e.g., student tiers, free tiers). Exported isFlatRateModel allows direct unit test coverage without routing side-effects. | Yes | agent |
| D024 | M004-erchk5/S02/T02 | provider | ollama_manage pull subcommand response format | Use stream:false in POST /api/pull body to get a single JSON response | Ollama pull default returns NDJSON progress lines. stream:false collapses the operation to a single status JSON, which is easier to surface in tool output without needing NDJSON parsing in the tool implementation. | Yes | agent |
