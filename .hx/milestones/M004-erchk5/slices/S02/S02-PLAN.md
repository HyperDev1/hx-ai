# S02: Ollama Native Provider + Flat-rate Routing Guard

**Goal:** Port Ollama native /api/chat provider (5f3c90bd2 + 8f6d879cc authMode fix), flat-rate routing guard for GitHub Copilot (6295d2a17 + 9f12c6286 hardening), model re-apply race guard (fad87b732), and pi-coding-agent model fallback race fix (992543921). All GSD→HX naming.
**Demo:** After this: After this: Ollama native provider registered; flat-rate guard active; model fallback race fixed; tsc clean

## Tasks
