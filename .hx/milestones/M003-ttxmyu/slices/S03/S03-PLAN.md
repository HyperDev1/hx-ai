# S03: Context Optimization (Masking + Phase Anchors)

**Goal:** Port context-masker.ts (observation masking) and phase-anchor.ts (phase boundary handoff); wire into system-context.ts injection and auto-mode preferences; port related preference fields (contextMasking, phaseAnchors)
**Demo:** After this: After this: context-masker.ts and phase-anchor.ts exist; tests pass; phase-anchor.json written in auto-mode session

## Tasks
